# Copilot Instructions

## Project Guidelines
- For this .NET Terminal.Gui file manager, prioritize simple, clean, readable, reasonably extensible implementations; treat performance as key, warn before meaningful performance costs, and handle potential exceptions so the application does not crash.